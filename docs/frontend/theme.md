# theme

## Files

- `theme.ts` — exports `navixaTheme`, a single MUI `createTheme()` call defining the brand palette (navy primary `#0B3D91`, light background), typography (Inter font, bold headings), and component style overrides (flat `AppBar`/`Drawer`, no-transform buttons, bordered cards/tables, custom selected-nav-item highlight).
